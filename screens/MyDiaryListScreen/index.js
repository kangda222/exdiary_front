import {connect} from 'react-redux';
import Action from './action';


// component의 props에 매핑할 상태를 정의
// 기존 React Component 내부의 constuctor()

const mapStateToProps = (state, ownProps) => {

};

const mapDispatchToProps = (dispatch, ownProps) => {
    return {
        
    };
};